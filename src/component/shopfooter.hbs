<footer>
    {{#each footer}}
    <div class="row my-3">
        {{#each this}}
        <div class="col-3">
            <a class="btn btn-link nav-link text-primary" href={{link}}>{{text}}</a>
        </div>

        {{/each}}
    </div>
    {{/each}}
</footer>